// Shared geometry for the hero terminal (all in the terminal's own `em`).
export const SCREEN_H_SMALL = 8.066; // screen height at rest in the hero (10.9 × 0.74)
export const SCREEN_FS = 0.74; // screen text size relative to the device
export const SCREEN_H_BIG = 16.3; // screen height once the device takes the stage (desktop)
